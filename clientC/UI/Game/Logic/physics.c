// Physics module implementation (skeleton).
#include "physics.h"

void physics_step(Player* p, const InputState* in, const MapView* map, float dt) {
    (void)p; (void)in; (void)map; (void)dt;
    // Implementation will come in a later step.
}